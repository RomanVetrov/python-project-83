name: Python CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@76f960c585d5bb4924b9a105222846fe4939a4cc
      - run: uv sync --extra dev
      - name: Run lint
        run: make lint
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@5bf5fdf1b1fb96ef97d855b7368d334fe1f23a79
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=RomanVetrov_python-project-83
            -Dsonar.organization=romanvetrov
            -Dsonar.sources=.
            -Dsonar.python.version=3.12
            -Dsonar.exclusions=tests/**
